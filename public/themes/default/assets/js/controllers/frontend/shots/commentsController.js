app.controller("commentsController", ["$scope","shotFactory", "$timeout","$window",

    function($scope, shotFactory, $timeout, $window) {

        $scope.comment = function($id){

        }
    }

]);